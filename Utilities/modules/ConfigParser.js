/**
 * Same some configs in a local JSOn and load them
 */
class ConfigParser {
    /**
     * Name of the config json
     * @param {string} name 
     */
    constructor (name) {
        this.cache = {};
    }

    /**
     * Load the JSON requested
     */
    load () {

    }

    /**
     * Save the cache to the JSON
     */
    save () {

    }
}

module.exports = ConfigParser;
